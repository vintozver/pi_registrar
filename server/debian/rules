#!/usr/bin/make -f
%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_install:
	dh_auto_install
	install -D -m 0644 debian/server-config.yaml debian/pi-registrar-server/usr/share/pi-registrar/server/config.yaml
